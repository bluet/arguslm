# =============================================================================
# ArgusLM Configuration
# =============================================================================
# Copy this file to .env and configure before starting:
#   cp .env.example .env
#
# Generate all secrets at once (choose one method):
#
# Method 1 - Python script (if you have cryptography installed):
#   python scripts/generate-secrets.py >> .env
#
# Method 2 - Docker one-liner (no local Python needed):
#   docker run --rm python:3.11-slim sh -c "pip install -q cryptography && python -c '
#     from cryptography.fernet import Fernet
#     import secrets
#     print(f\"ENCRYPTION_KEY={Fernet.generate_key().decode()}\")
#     print(f\"SECRET_KEY={secrets.token_urlsafe(32)}\")
#     print(f\"DB_PASSWORD={secrets.token_urlsafe(24)}\")
#   '" >> .env
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED SECRETS (must generate before first run)
# -----------------------------------------------------------------------------

# Fernet key for encrypting provider API keys at rest (44 chars, base64)
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Application secret key for signing tokens
# Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=

# PostgreSQL password
# Generate: python -c "import secrets; print(secrets.token_urlsafe(24))"
DB_PASSWORD=

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------

DB_USER=arguslm
DB_NAME=arguslm
# Note: DATABASE_URL is constructed in docker-compose.yml from DB_* vars

# -----------------------------------------------------------------------------
# Backend Configuration
# -----------------------------------------------------------------------------

PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# LiteLLM logging level (DEBUG, INFO, WARNING, ERROR)
LITELLM_LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------

NODE_ENV=production

# API URL for frontend to reach backend (change for production deployment)
# For local development: http://localhost:8000
# For production: https://your-domain.com or https://api.your-domain.com
VITE_API_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# CORS Configuration (optional)
# -----------------------------------------------------------------------------

# Comma-separated list of allowed origins (for production, set to your domain)
# CORS_ORIGINS=https://your-domain.com

# -----------------------------------------------------------------------------
# Provider API Keys (configure via UI, or set here for auto-discovery)
# -----------------------------------------------------------------------------

# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_API_KEY=...
